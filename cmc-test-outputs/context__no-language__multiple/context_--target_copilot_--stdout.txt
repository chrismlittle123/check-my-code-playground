================================================================================
CMC TEST OUTPUT
================================================================================

METADATA
--------
Timestamp:    2025-12-05 10:32:44
Project:      /Users/christopherlittle/Documents/GitHub/personal/check-my-code-playground/projects/context/no-language/multiple
Command:      cmc context --target copilot --stdout
Description:  Generate Copilot context (stdout)
Exit Code:    0
Duration:     1.447129000s
CMC Version:  1.2.0

PROJECT FILES
-------------
total 8
drwx------@ 3 christopherlittle  staff   96  5 Dec 10:21 .
drwxr-xr-x@ 8 christopherlittle  staff  256  5 Dec 10:21 ..
-rw-------@ 1 christopherlittle  staff   98  5 Dec 10:21 cmc.toml

CMC.TOML CONTENTS
-----------------
[project]
name = "context-multiple-test"

[prompts]
templates = ["typescript/5.5", "python/3.12"]

STDOUT
------
## TypeScript 5.5 Coding Standards

You MUST follow these TypeScript 5.5 coding standards when writing or modifying code:

### Target Version

- Target TypeScript 5.5 specifically.
- Target Node.js 20 runtime.
- Use TypeScript 5.5 features where appropriate.

### Variable Declarations

- NEVER use `var`. Always use `const` for values that won't be reassigned, or `let` when reassignment is necessary.
- Prefer `const` over `let` whenever possible.

### Type Safety

- NEVER use `any` type. Use `unknown` if the type is truly unknown, then narrow it with type guards.
- Always provide explicit return types for functions.
- Use strict null checks - handle `null` and `undefined` explicitly.
- Enable `strict: true` in tsconfig.json.

### TypeScript 5.5 Features

- Use inferred type predicates for cleaner type narrowing.
- Use `const` type parameters where appropriate.
- Use `satisfies` operator for type validation without widening.
- Use `using` declarations for resource management (Explicit Resource Management).
- Use `import type` and `export type` for type-only imports/exports.

### Equality

- ALWAYS use strict equality (`===` and `!==`). Never use loose equality (`==` and `!=`).

### Error Handling

- Always handle errors explicitly. Never swallow errors silently.
- Use typed error handling where possible.
- Prefer `unknown` over `any` in catch clauses.

### Imports

- Use ES module imports (`import`/`export`), not CommonJS (`require`/`module.exports`).
- Sort imports: external dependencies first, then internal modules.
- Use `import type` for type-only imports.

### Node.js 20

- Use native fetch API (no need for node-fetch).
- Use native test runner where appropriate.
- Use ES modules (`"type": "module"` in package.json).


## Python 3.12 Coding Standards

You MUST follow these Python 3.12 coding standards when writing or modifying code:

### Target Version

- Target Python 3.12 specifically.
- Use Python 3.12 features where appropriate.

### Imports

- Remove all unused imports.
- Sort imports in the standard order: standard library, third-party, local.
- Use absolute imports over relative imports.

### Code Style

- Maximum line length: 100 characters.
- Use f-strings for string formatting, not `.format()` or `%` formatting.

### Python 3.12 Features

- Use `match` statements for complex conditionals.
- Use type unions with `|` syntax (e.g., `str | None` instead of `Optional[str]`).
- Use `type` statement for type aliases (PEP 695): `type Vector = list[float]`.
- Use f-string debugging with `=` (e.g., `f"{x=}"` for `x=42`).
- Use `@override` decorator from `typing` for method overrides.

### Type Hints

- Add type hints to all function signatures.
- Use built-in generics (`list[str]`, `dict[str, int]`) instead of `typing.List`, `typing.Dict`.
- Use `from __future__ import annotations` is no longer needed in 3.12.

### Error Handling

- Be specific with exception types. Avoid bare `except:` clauses.
- Use exception groups and `except*` for handling multiple exceptions where appropriate.
- Use context managers (`with` statements) for resource management.

### Naming

- Use `snake_case` for functions and variables.
- Use `PascalCase` for classes.
- Use `SCREAMING_SNAKE_CASE` for constants.


STDERR
------

================================================================================
